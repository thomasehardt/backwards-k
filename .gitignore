# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Env / secrets
.env
.env.local
.env.*.local

# Logs
*.log

# mobile/ (Capacitor + Android project) has its own .gitignore for
# node_modules/, derived www/, Gradle build output, and keystores — the
# Android project itself IS checked in (unlike a typical `npx cap add`
# setup) so CI can build it reproducibly.
